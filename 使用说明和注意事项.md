# 电梯管理系统 — 使用说明与注意事项

> 版本: 1.0.0 | 更新日期: 2026-05-21

---

## 一、系统概述

电梯管理系统（Elevator System）是一套面向物业管理公司的电梯运维管理平台，提供电梯档案管理、维修工单、巡检任务、维保计划、合同管理、月费核算等全流程数字化管理功能。

### 系统架构

```
┌──────────┐    ┌──────────┐    ┌──────────────┐
│  Nginx   │───▶│ Web Admin│    │  PostgreSQL   │
│ (反向代理) │    │ (Vue 3)  │    │  (主数据库)    │
└────┬─────┘    └──────────┘    └──────────────┘
     │                          ┌──────────────┐
     └─────────────────────────▶│    Redis     │
                               │  (缓存/队列)   │
                               └──────────────┘
```

| 组件 | 技术栈 | 端口 |
|------|--------|------|
| API 服务 | NestJS 11 + Prisma 5.22 | 3000 |
| 管理后台 | Vue 3 + Vite 6 | 5174 (dev) / 80 (prod) |
| 数据库 | PostgreSQL 16 | 5432 |
| 缓存 | Redis 7 | 6379 |
| 反向代理 | Nginx | 80 |

---

## 二、快速开始

### 2.1 环境要求

- Node.js >= 20
- PostgreSQL >= 16
- Redis >= 7 (可选,缓存降级不影响核心功能)
- npm >= 9

### 2.2 本地开发

```bash
# 1. 配置数据库
brew services start postgresql@18
createdb elevator_system

# 2. 配置环境变量
cd server
cp .env.example .env
# 编辑 .env，填入正确的 DATABASE_URL

# 3. 安装依赖并构建
npm install
npx prisma generate
npx prisma migrate deploy
npm run build

# 4. 填充测试数据
DATABASE_URL="postgresql://elevator:elevator_pass_2024@localhost:5432/elevator_system" \
  node dist/src/seed.js

# 5. 启动 API 服务
npm run start:prod

# 6. 启动管理后台 (新终端)
cd web-admin
npm install
npx vite --port 5174
```

### 2.3 Docker 部署

```bash
# 开发环境
docker compose up -d

# 生产环境
docker compose -f docker-compose.prod.yml up -d
```

### 2.4 测试账号

| 角色 | 手机号 | 密码 | 说明 |
|------|--------|------|------|
| 系统管理员 | 13800000000 | admin123 | 最高权限 |
| 项目经理 | 13900000001 | 123456 | 张经理 |
| 客服人员 | 13900000002 | 123456 | 王客服 |
| 工程师 | 13900000003 | 123456 | 李工程 |
| 维保人员 | 13900000004 | 123456 | 赵维保(单位1) |
| 维保人员 | 13900000005 | 123456 | 钱维保(单位2) |
| 安全员 | 13900000006 | 123456 | 周安全 |
| 主管 | 13900000007 | 123456 | 吴主管 |

---

## 三、功能模块说明

### 3.1 认证与权限

- 登录使用手机号+密码，返回 JWT Token（有效期7天）
- 微信小程序登录（预留接口）
- Token 需在请求头中以 `Authorization: Bearer <token>` 传递
- 角色权限系统：ADMIN > PROJECT_MANAGER > ENGINEER > ELEVATOR_MAINTAINER 等

### 3.2 仪表盘

提供多维度数据可视化：
- 电梯运行状态统计（运行/停止/故障/维保）
- 待处理工单数量
- 报修趋势图（近6个月）
- 故障类型分布
- 维保/维修时长统计
- 非计划性维保统计

### 3.3 电梯管理

- 电梯档案 CRUD（增删改查）
- 电梯注册代码、类型、载重、速度等参数管理
- 按项目、楼栋筛选
- 即将到期巡检提醒
- Excel 批量导入/导出

### 3.4 合同管理

- 合同生命周期管理
- 合同与电梯关联（多对多）
- 合同配件管理
- 合同履约评价
- 即将到期提醒（可配置，默认30天）
- Excel 导入/导出

### 3.5 维修工单

- 故障报修 → 接单 → 维修 → 完成全流程
- 工单状态流转：PENDING → PROCESSING → COMPLETED → CLOSED
- 支持维修配件记录和费用核算
- 维修媒体文件上传（图片/视频）
- 推荐配件查询
- 维修资金拨付文档生成
- 备件库存预警
- 维修报告导出

### 3.6 巡检管理

- 巡检任务创建与分配
- 巡检照片上传
- 巡检记录查询
- 巡检数据导出

### 3.7 维保计划

- 维保计划创建（支持批量生成）
- 计划类型：半月保/月保/季度保/半年保/年保
- 维保人员分配（可多人）
- 计划状态跟踪（待执行/已完成/已逾期）
- 支持导入/导出

### 3.8 月费管理

- 月度费用自动生成
- 费用项明细管理
- 缴费状态跟踪
- 数据导入/导出

### 3.9 组织管理

- 物业集团管理
- 项目管理（小区）
- 楼栋管理
- 用户管理（员工账号）

### 3.10 维保单位管理

- 维保公司信息管理
- 维保单位评分体系
- 评分自动计算与重算

### 3.11 QR 码

- 电梯二维码生成（每个电梯独立）
- 二维码重新生成
- 扫码内容格式：`elevator://scan?elevatorId={id}`

### 3.12 通知与工作流

- 通知列表与未读计数
- 批量已读标记
- 维修工单审批工作流
- 维修材料管理流程

---

## 四、API 接口总览

系统共注册 **85 个 REST API 端点**，Swagger 文档地址：

- 本地开发: `http://localhost:3000/api/docs`
- 生产环境: `http://your-domain/api/docs`

### 核心接口分组

| 分组 | 前缀 | 接口数 | 主要功能 |
|------|------|--------|----------|
| 认证 | `/api/auth` | 3 | 登录、微信登录、个人信息 |
| 组织 | `/api/organizations` | 2 | 组织增改查 |
| 项目 | `/api/projects` | 4 | 项目增删改查 |
| 用户 | `/api/users` | 6 | 用户管理、层级、密码 |
| 电梯 | `/api/elevators` | 8 | 电梯CRUD、导入导出、待巡检 |
| 合同 | `/api/contracts` | 14 | 合同CRUD、电梯关联、配件、评价 |
| 维修 | `/api/repairs` | 16 | 工单CRUD、接单、配件、费用、导出 |
| 巡检 | `/api/inspections` | 4 | 巡检创建、照片、导出 |
| 维保计划 | `/api/maintenance-plans` | 7 | 计划CRUD、批量、导入 |
| 维保单位 | `/api/maintenance-units` | 7 | 单位CRUD、评分 |
| 月费 | `/api/monthly-fees` | 7 | 生成、查询、统计、导入导出 |
| 二维码 | `/api/qrcodes` | 4 | 生成、查询、重新生成 |
| 通知 | `/api/notifications` | 4 | 列表、未读、标记已读 |
| 工作流 | `/api/workflows` | 6 | 审批、材料管理 |
| 文件 | `/api/files` | 3 | 上传、下载 |
| 仪表盘 | `/api/dashboard` | 8 | 多维度数据统计 |

---

## 五、部署指南

### 5.1 生产环境部署 (QNAP NAS)

系统通过 GitHub Actions CI/CD 自动部署到 QNAP NAS。

```yaml
# 触发方式: 推送 main 分支
on:
  push:
    branches: [main]
```

部署流程：
1. GitHub Actions 检出代码
2. 在 NAS 上执行 `docker compose -f docker-compose.prod.yml up -d --build`
3. 容器自动重建（API、Admin、Nginx）
4. 数据库迁移自动执行（`npx prisma migrate deploy`）
5. 种子数据自动填充（`node prisma/seed.js`）
6. 烟雾测试验证部署

### 5.2 容器说明

| 容器 | 镜像 | 健康检查 | 重启策略 |
|------|------|----------|----------|
| elevator-api | 自构建 (node:22-alpine) | curl localhost:3000/api | unless-stopped |
| elevator-admin | 自构建 (nginx:alpine) | 无 | unless-stopped |
| elevator-postgres | postgres:16-alpine | pg_isready | unless-stopped |
| elevator-redis | redis:7-alpine | redis-cli ping | unless-stopped |
| elevator-nginx | nginx:alpine | 无 | unless-stopped |

### 5.3 环境变量说明

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `DATABASE_URL` | PostgreSQL 连接串 | `postgresql://elevator:pass@postgres:5432/elevator_system` |
| `JWT_SECRET` | JWT 签名密钥 | `change_this_in_production` |
| `REDIS_URL` | Redis 连接串 | `redis://:pass@redis:6379` |
| `NODE_ENV` | 运行环境 | `production` |
| `UPLOAD_PATH` | 文件上传路径 | `/app/uploads` |
| `QR_URL_PREFIX` | 二维码 URL 前缀 | `/uploads/qrcodes` |
| `INSPECTION_WINDOW_DAYS` | 巡检到期提醒天数 | `30` |
| `CONTRACT_WINDOW_DAYS` | 合同到期提醒天数 | `30` |
| `MAINTENANCE_PLAN_WINDOW_DAYS` | 维保计划提醒天数 | `7` |

---

## 六、数据库

### 6.1 数据表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| User | 用户表 | phone, password, role, maintenanceUnitId |
| Organization | 物业集团表 | name, address |
| Project | 项目/小区表 | name, organizationId |
| Building | 楼栋表 | name, projectId |
| Elevator | 电梯表 | elevatorNo, regCode, status, buildingId |
| Contract | 合同表 | contractNo, status, startDate, endDate |
| ContractElevator | 合同-电梯关联 | contractId, elevatorId |
| RepairOrder | 维修工单 | orderNo, status, elevatorId, faultDescription |
| MaintenancePlan | 维保计划 | planType, planDate, status, maintainerIds |
| InspectionTask | 巡检任务 | inspectionType, status, elevatorId |
| MonthlyFee | 月费 | month, totalAmount, status |
| QRCode | 二维码 | elevatorId, code, qrImagePath |
| Notification | 通知 | title, content, userId, isRead |
| WorkflowInstance | 工作流实例 | repairOrderId, status |
| WorkflowNode | 工作流节点 | workflowInstanceId, approverId, action |
| MaintenanceUnit | 维保单位 | name, contactPerson, score |
| RepairPart | 维修配件 | name, quantity, unitPrice |
| RepairCost | 维修费用 | type, amount, description |
| FaultRecord | 故障记录 | elevatorId, faultType, description |
| FundMaterial | 维修资金材料 | repairOrderId, docPath |

### 6.2 迁移管理

```bash
# 创建新迁移
npx prisma migrate dev --name 迁移描述

# 部署迁移
npx prisma migrate deploy

# 重置数据库
npx prisma migrate reset
```

---

## 七、CI/CD 流程

### 7.1 持续集成 (CI)

每次推送到 `main` 或 `develop` 分支，以及创建 PR 时触发：

| 步骤 | 说明 |
|------|------|
| Server 构建与测试 | Node 22, PostgreSQL 16 服务容器 |
| 依赖安装 | npm ci |
| Prisma 生成 | npx prisma generate |
| 迁移部署 | npx prisma migrate deploy |
| TypeScript 类型检查 | npx tsc --noEmit |
| 单元测试 | npx jest --coverage |
| 生产构建 | npm run build |
| Web Admin 构建与检查 | vue-tsc, eslint, vite build |

### 7.2 持续部署 (CD)

推送到 `main` 分支触发自动部署到 QNAP NAS：
1. 等待 CI 通过（可选）
2. SSH 到 NAS 执行 Docker Compose 重建
3. 数据库迁移
4. 种子数据
5. 烟雾测试验证

---

## 八、测试验证摘要

以下为本地验证通过的测试项（2026-05-21 验证）：

### 8.1 API 端点测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 管理员登录 | ✅ | 13800000000/admin123 → JWT Token |
| 错误密码登录 | ✅ | 返回 401 + "手机号或密码错误" |
| 个人信息获取 | ✅ | 返回用户详情 |
| 项目经理登录 | ✅ | 13900000001/123456 → Token |
| 维保人员登录 | ✅ | 13900000004/123456 → Token |
| 组织列表 | ✅ | 返回 2 个物业集团 |
| 项目列表 | ✅ | 返回 3 个项目 |
| 电梯列表 | ✅ | 返回 10 部电梯（按权限过滤） |
| 电梯详情 | ✅ | 返回电梯完整信息 |
| 合同列表 | ✅ | 返回 4 份合同 |
| 仪表盘概览 | ✅ | 返回电梯统计、待处理工单等 |
| 报修趋势 | ✅ | 返回近 6 个月趋势数据 |
| 故障分布 | ✅ | 返回故障分类统计 |
| 维修工单列表 | ✅ | 返回 5 个工单 |
| 维修工单详情 | ✅ | 返回工单完整信息 |
| 巡检列表 | ✅ | 返回 5 条巡检记录 |
| 维保计划列表 | ✅ | 返回 20+ 条维保计划 |
| QR 码列表 | ✅ | 返回 10 个二维码记录 |
| 维保单位列表 | ✅ | 返回 4 个维保单位 |
| 即将巡检电梯 | ✅ | 返回 2 条即将巡检提醒 |
| 创建组织 | ✅ | POST /api/organizations 通过 |
| Swagger 文档 | ✅ | 85 个端点完整文档化 |

### 8.2 前端测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| Vite 构建 | ✅ | 3.02s 完成 |
| API 代理 | ✅ | Vite proxy → localhost:3000 |
| 前端资源加载 | ✅ | Vue SFC 正确编译 |
| 打包体积 | ✅ | 约 2MB (gzip 约 650KB) |

### 8.3 基础设施

| 组件 | 版本 | 状态 |
|------|------|------|
| Node.js | 26.0 | ✅ |
| PostgreSQL | 18.4 | ✅ |
| Redis | 7.x | ✅ |
| NestJS | 11.x | ✅ |
| Prisma | 5.22 | ✅ |

---

## 九、注意事项

### ⚠️ 运行环境

1. **Node.js 版本**：当前使用 Node.js 26，部署时需确保自托管运行器的 Node.js 版本兼容
2. **PostgreSQL 密码**：`.env` 中的密码需与 `docker-compose.yml` 中 `DB_PASSWORD` 一致
3. **Redis 密码**：同 Docker Compose 中的 `REDIS_PASSWORD`；本地 Homebrew Redis 默认无密码，API 日志会提示 `user does not require a password`，功能不受影响
4. **JWT 密钥**：生产环境必须修改 `JWT_SECRET`，使用强随机字符串（>=32字符）
5. **时区**：默认 `Asia/Shanghai`，如需变更需修改 `docker-compose.yml` 中的 `TZ` 环境变量

### ⚠️ 数据库

6. **Migration 不可逆**：Prisma 迁移仅支持 `deploy`（应用待处理迁移），不支持回滚。如需回退，需手动编写反向迁移
7. **种子数据**：每次部署 `docker-entrypoint.sh` 自动执行 `node prisma/seed.js`。种子采用 `upsert` 方式，会覆盖已存在的用户密码哈希。**已有用户密码将在每次部署后被重置为默认值**
8. **表名区分大小写**：PostgreSQL 表名使用双引号包裹（Prisma 默认行为），直接 SQL 查询时需注意大小写
9. **UUID vs 自增 ID**：主键使用 CUID（`cmpfiv86w0000tdaw2lqm519l` 格式），非自增整数

### ⚠️ 运维

10. **自托管 Runner 离线**：部署依赖 NAS 上的 GitHub Actions 自托管 Runner。如果 NAS 关机或 Runner 离线，部署任务将保持 `queued` 状态直到 Runner 恢复
11. **Docker 健康检查**：`docker-compose.prod.yml` 中 API 容器配置了健康检查（curl localhost:3000/api），依赖 `curl` 命令（由 Dockerfile 安装）。如果健康检查失败，容器会被自动重启
12. **Docker 端口冲突**：生产环境 nginx 使用 `80:80`，确保 NAS 上 80 端口未被其他服务占用；PostgreSQL 和 Redis 仅监听 `127.0.0.1`，不对外暴露
13. **日志持久化**：Nginx 日志挂载到 `elevator_logs` 卷，用于排查访问和错误
14. **上传文件**：上传路径（`elevator_uploads` 卷）持久化保存，容器重建不会丢失
15. **磁盘空间**：镜像存储和数据库日志可能随时间增长，建议定期清理未使用的 Docker 镜像和数据库 WAL 日志

### ⚠️ 安全

16. **密码策略**：当前仅前端控制密码复杂度；API 层无密码强度校验。生产环境建议增加
17. **HTTPS**：默认 HTTP 模式，生产环境需在 nginx 配置 SSL 证书启用 HTTPS
18. **请求限流**：NestJS 内置 `ThrottlerModule` 进行请求频率限制，但阈值可能需要根据实际使用量调整
19. **CORS**：当前 CORS 配置允许所有来源（`origin: true`），生产环境应限制为具体域名
20. **Helmet**：安全头已启用（helmet middleware），提供了基本的 HTTP 安全防护

### ⚠️ 功能限制

21. **Redis 降级**：如果 Redis 不可用，缓存服务会静默降级，所有操作直接查数据库，不影响核心功能
22. **文件上传**：通过 `multer` 处理，存储到本地文件系统。水平扩展时需改用对象存储（如 S3/MinIO）
23. **WebSocket**：未集成 WebSocket，实时通知需前端轮询
24. **微信登录**：`/api/auth/wechat-login` 接口已预留，但未对接微信 OAuth 流程，需配置小程序 AppID/Secret
25. **导出格式**：Excel 导出使用 `xlsx` 库，支持 `.xlsx` 格式；合同和工单支持 Word 文档生成
26. **维修资金文档**：`/api/workflows/:id/generate-material-doc` 生成维修资金拨付相关文档，需安装字体支持中文

---

## 十、常见问题

### Q: 登录返回 401 "手机号或密码错误"
**A:** 确保种子数据已执行。首次部署或数据库重置后需运行 `node prisma/seed.js`。如果升级部署后仍遇此问题，检查 `seed.ts` 中 upsert 的 `update` 字段是否包含 `password`。

### Q: API 启动报 "Cannot connect to database"
**A:** 检查 DATABASE_URL 配置和 PostgreSQL 服务是否运行。Docker 环境检查 `docker compose ps` 确认 postgres 容器健康。

### Q: 自托管 Runner 不执行部署
**A:** 在 GitHub → Settings → Actions → Runners 页面检查 Runner 状态。如果离线，需重启 Runner 服务或检查 NAS 网络连接。

### Q: 前端页面白屏
**A:** 检查 Vite 代理配置是否正确。如果是生产环境，检查 Nginx 配置是否正确代理到 admin 容器。
