version: '3.8'

services:
  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  api:
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - ./server/src:/app/src:ro

  admin:
    ports:
      - "8080:80"
